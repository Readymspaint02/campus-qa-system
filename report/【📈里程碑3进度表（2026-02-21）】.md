# 里程碑3进度表（2026-02-21）

## 1. 里程碑目标
根据“开题对齐版方案”，里程碑3目标为：
1. 全链路稳定化（参数校验、错误处理、边界保护）
2. 小程序侧体验收口（个人中心能力补齐）
3. 联调收尾并沉淀验收证据

## 2. 进度总览

| 项目 | 状态 | 完成度 |
| --- | --- | --- |
| 里程碑3 | 已完成 | 100% |

## 3. 任务明细

| 序号 | 任务 | 结果 | 证据文件 |
| --- | --- | --- | --- |
| 1 | mini 接口分页参数标准化（pageNo/pageSize） | 已完成 | `jeecg-boot/jeecg-boot-module/jeecg-boot-module-campusqa/src/main/java/org/jeecg/modules/campusqa/mini/controller/CampusQaMiniController.java` |
| 2 | `knowledge/detail` 增加 `id` 必填校验 | 已完成 | 同上 |
| 3 | `feedback` 增加内容必填与长度限制（500） | 已完成 | 同上 |
| 4 | `history` 增加最小有效参数校验（query/knowledgeId 至少其一） | 已完成 | 同上 |
| 5 | `subscribe/toggle` 增加参数约束（deptCode/categoryId 至少其一） | 已完成 | 同上 |
| 6 | 新增 `GET /campusqa/mini/subscribe/list` | 已完成 | 同上 |
| 7 | `ask` 增加 question 必填保护 | 已完成 | 同上 |
| 8 | 小程序请求层统一错误处理（HTTP/业务失败统一拦截） | 已完成 | `campus-qa-miniapp/utils/request.js` |
| 9 | 小程序个人中心新增“我的订阅”展示 | 已完成 | `campus-qa-miniapp/pages/profile/index.js` `campus-qa-miniapp/pages/profile/index.wxml` |
| 10 | 后端构建与安装（保证 system-start 运行拿到最新 campusqa 代码） | 已完成 | 命令：`mvn -pl :jeecg-system-biz -am -DskipTests install` |
| 11 | 里程碑3联调脚本 | 已完成 | `report/m3_integration.ps1` |
| 12 | 联调结果落盘 | 已完成 | `report/m3-api-results.json` `report/backend-start-m3.log` |

## 4. 本次关键结果
1. 新增订阅列表接口已可用，并在小程序个人中心可见。
2. mini 核心写接口已补齐参数防呆，避免无效请求写入脏数据。
3. 统计接口联调继续通过，历史与 ask 数据可累计。
4. 启动与联调流程可脚本化复现（`m3_integration.ps1`）。

## 5. 验收结论
- 里程碑3：**通过**
- 建议下一步：进入“答辩材料打包阶段”（截图、演示脚本、论文实现章节同步）。

 我来为你制定一份完整的**艺术品在线交易平台开发全流程保姆级教程**，基于你上传的5个核心文档，从环境搭建到最终交付。

---

## 🎯 整体思路：5文档作战体系

```
【5文档关系图】
┌─────────────────────────────────────────────────────────┐
│  文档1《系统架构与核心决策》= 技术宪法（Why/What/Risk）   │
│  ↓ 输入：架构图、技术选型、核心代码模板                   │
├─────────────────────────────────────────────────────────┤
│  文档2《数据模型与状态机》= 数据地基（状态机、SQL、公式）   │
│  ↓ 输入：ER图、状态流转、日终对账SQL                      │
├─────────────────────────────────────────────────────────┤
│  文档3《API与接口契约》= 通信协议（REST+WebSocket规范）    │
│  ↓ 输入：接口签名、错误码、权限矩阵                       │
├─────────────────────────────────────────────────────────┤
│  文档4《编码规范与实现模板》= 施工图纸（Vue3/Java模板）     │
│  ↓ 输入：可直接复制的代码模板、检查清单                   │
├─────────────────────────────────────────────────────────┤
│  文档5《验收红线与自动化》= 质量门禁（上线前必过3条红线）   │
│  ↓ 产出：验收报告、可执行脚本                            │
└─────────────────────────────────────────────────────────┘
```

**使用原则**：开发时**文档4**在手边，设计时**文档2**在手边，联调时**文档3**在手边，上线前**文档5**在手边，**文档1**全程指导架构决策。

---

## 📋 全流程开发路线图（12周）

| 阶段         | 周次       | 核心任务                       | 产出物                      | 依据文档                 |
| :----------- | :--------- | :----------------------------- | :-------------------------- | :----------------------- |
| **战前准备** | Week 1-2   | 环境搭建+脚手架部署+数据库设计 | 环境检查报告、ER图、SQL文件 | 文档1第2章、文档2第1-2章 |
| **开发阶段** | Week 3-6   | 前后端并行开发                 | 功能模块代码、接口文档      | 文档3、文档4             |
| **联调测试** | Week 7-8   | 接口联调+并发测试              | 测试报告、Bug清单           | 文档3、文档5             |
| **验收上线** | Week 9-10  | 红线检查+性能优化              | 验收报告、部署文档          | 文档5                    |
| **论文撰写** | Week 11-12 | 毕设文档整理                   | 毕业论文、演示视频          | 全部文档                 |

---

## 🔧 Week 1-2: 战前准备层（保姆级步骤）

### Day 1: 环境安装（严格按版本）

**Step 1: 安装基础软件（按顺序）**

```powershell
# 1. Node.js 18.x LTS（前端运行环境）
# 下载地址：https://nodejs.org/dist/v18.20.2/node-v18.20.2-x64.msi
# 验证：
node -v  # 输出：v18.20.2
npm -v   # 输出：10.5.0

# 2. JDK 17 LTS（后端运行环境）
# 下载地址：https://www.oracle.com/java/technologies/downloads/#java17
# 或使用 OpenJDK：https://adoptium.net/temurin/releases/?version=17
# 验证：
java -version  # 输出：openjdk version "17.0.x"

# 3. Maven 3.8.x（后端构建工具）
# 下载地址：https://maven.apache.org/download.cgi
# 解压后配置环境变量MAVEN_HOME
# 验证：
mvn -v  # 输出：Apache Maven 3.8.x

# 4. MySQL 8.0（关系型数据库）
# 下载地址：https://dev.mysql.com/downloads/installer/
# 安装时选择：Server + Workbench + Connector/J
# 验证：
mysql --version  # 输出：mysql Ver 8.0.x

# 5. Redis 7.0（缓存+分布式锁）
# Windows版：https://github.com/tporadowski/redis/releases
# 或Docker：docker run -d -p 6379:6379 redis:7.0
# 验证：
redis-cli ping  # 输出：PONG

# 6. MongoDB 5.x（非结构化数据-溯源信息）
# 下载地址：https://www.mongodb.com/try/download/community
# 验证：
mongosh --version  # 输出：5.x.x

# 7. RabbitMQ 3.12+（消息队列）
# 需要先安装Erlang：https://www.erlang.org/downloads
# 下载地址：https://www.rabbitmq.com/download.html
# 验证：访问 http://localhost:15672 默认账号guest/guest

# 8. Elasticsearch 7.x（搜索引擎）
# 下载地址：https://www.elastic.co/downloads/past-releases/elasticsearch-7-17-0
# 验证：curl http://localhost:9200

# 9. Nginx 1.24（反向代理+静态资源）
# 下载地址：https://nginx.org/en/download.html
# 验证：nginx -v  # 输出：nginx version: nginx/1.24.x
```

**产出物1：《环境检查报告》**
```markdown
# 环境检查报告
日期：2026-02-19
检查人：杨景煊

| 组件 | 版本要求 | 实际版本 | 状态 |
|:---|:---|:---|:---|
| Node.js | 18.x | v18.20.2 | ✅ |
| JDK | 17 | 17.0.8 | ✅ |
| Maven | 3.8+ | 3.9.6 | ✅ |
| MySQL | 8.0 | 8.0.36 | ✅ |
| Redis | 7.0 | 7.0.15 | ✅ |
| MongoDB | 5.x | 5.0.24 | ✅ |
| RabbitMQ | 3.12+ | 3.12.12 | ✅ |
| Elasticsearch | 7.x | 7.17.15 | ✅ |
| Nginx | 1.24 | 1.24.0 | ✅ |

结论：环境就绪，可进行下一步
```

---

### Day 2: 脚手架部署（GitHub模板）

**Step 2: 前端脚手架（vben-admin）**

```bash
# 1. Fork或Clone vben-admin
git clone https://github.com/vbenjs/vue-vben-admin.git artwork-frontend
cd artwork-frontend

# 2. 切换到稳定版本（不要用main分支）
git checkout v2.11.0

# 3. 安装依赖（使用pnpm，如果没有先装：npm install -g pnpm）
pnpm install

# 4. 启动开发服务器
pnpm dev

# 5. 验证：浏览器访问 http://localhost:5173
# 默认账号：vben / 123456
```

**Step 3: 后端脚手架（mall）**

```bash
# 1. Clone mall项目
git clone https://github.com/macrozheng/mall.git artwork-backend
cd artwork-backend

# 2. 检出稳定版本
git checkout v1.0.0

# 3. 导入IDEA（IntelliJ IDEA 2023.x）
# File → Open → 选择artwork-backend/mall-admin/pom.xml

# 4. 修改数据库配置（application-dev.yml）
# 路径：mall-admin/src/main/resources/application-dev.yml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/art_platform?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: your_password

# 5. 创建数据库
mysql -u root -p
CREATE DATABASE art_platform CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# 6. 导入mall基础表结构
# 文件位置：document/sql/mall.sql
mysql -u root -p art_platform < document/sql/mall.sql
```

**产出物2：《脚手架部署记录》**
```markdown
# 脚手架部署记录

## 前端（vben-admin v2.11.0）
- 仓库：artwork-frontend
- 启动命令：pnpm dev
- 访问地址：http://localhost:5173
- 改造计划：删除无关模块，新增竞拍相关页面

## 后端（mall v1.0.0）
- 仓库：artwork-backend
- 启动方式：IDEA运行 MallAdminApplication
- 访问地址：http://localhost:8080
- 改造计划：商品→藏品，订单→艺术品交易订单，新增竞拍模块
```

---

### Day 3-5: 数据库设计（核心！）

**依据文档2《数据模型与状态机》第1章+第5章DDL**

**Step 4: 绘制ER图（使用Navicat或dbdiagram.io）**

```sql
-- 核心表结构（基于文档2的17张表清单）
-- 执行顺序：先用户/基础表 → 藏品表 → 交易表 → 资金表

-- ============================================
-- 1. 用户域（文档2 第1.3节）
-- ============================================

CREATE TABLE `t_user` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键',
    `username` VARCHAR(64) NOT NULL COMMENT '用户名',
    `phone` VARCHAR(20) NOT NULL COMMENT '手机号',
    `password` VARCHAR(128) NOT NULL COMMENT '加密密码',
    `user_type` TINYINT NOT NULL DEFAULT 1 COMMENT '1-买家 2-个人卖家 3-机构卖家 4-管理员',
    `status` TINYINT NOT NULL DEFAULT 1 COMMENT '1-正常 2-冻结 3-注销',
    `avatar` VARCHAR(255) DEFAULT NULL COMMENT '头像URL',
    `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_phone` (`phone`),
    UNIQUE KEY `uk_username` (`username`),
    KEY `idx_user_type` (`user_type`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';

CREATE TABLE `t_wallet` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
    `user_id` BIGINT UNSIGNED NOT NULL COMMENT '用户ID',
    `balance` DECIMAL(12,2) NOT NULL DEFAULT 0.00 COMMENT '可用余额',
    `frozen_amount` DECIMAL(12,2) NOT NULL DEFAULT 0.00 COMMENT '冻结金额',
    `status` TINYINT NOT NULL DEFAULT 1 COMMENT '1-正常 2-冻结',
    `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_user_id` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='钱包表';

-- ============================================
-- 2. 藏品域（文档2 第1.3节 + 第2.3节联合状态机）
-- ============================================

CREATE TABLE `t_category` (
    `id` INT UNSIGNED NOT NULL AUTO_INCREMENT,
    `name` VARCHAR(64) NOT NULL COMMENT '分类名称',
    `parent_id` INT UNSIGNED DEFAULT 0 COMMENT '父分类ID',
    `level` TINYINT NOT NULL DEFAULT 1 COMMENT '层级',
    `sort_order` INT DEFAULT 0,
    `icon` VARCHAR(255) DEFAULT NULL,
    PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='藏品分类';

CREATE TABLE `t_artwork` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
    `artwork_no` VARCHAR(32) NOT NULL COMMENT '藏品编号 ART2025xxxx',
    `seller_id` BIGINT UNSIGNED NOT NULL COMMENT '卖家ID',
    `title` VARCHAR(128) NOT NULL COMMENT '藏品名称',
    `category_id` INT UNSIGNED NOT NULL COMMENT '分类ID',
    `era` VARCHAR(32) DEFAULT NULL COMMENT '年代',
    `material` VARCHAR(64) DEFAULT NULL COMMENT '材质',
    `size_desc` VARCHAR(64) DEFAULT NULL COMMENT '尺寸描述',
    `description` TEXT COMMENT '藏品描述',
    `images` JSON COMMENT '图片URL数组',
    `transaction_type` TINYINT NOT NULL COMMENT '1-一口价 2-竞拍',
    `start_price` DECIMAL(12,2) DEFAULT NULL COMMENT '起拍价（竞拍）',
    `fixed_price` DECIMAL(12,2) DEFAULT NULL COMMENT '一口价',
    `increment` DECIMAL(10,2) DEFAULT NULL COMMENT '加价幅度（竞拍）',
    `status` TINYINT NOT NULL DEFAULT 0 COMMENT '0-草稿 1-待审核 2-审核通过 3-审核驳回 4-已上架 5-已售出 6-已下架 7-流拍',
    `view_count` INT DEFAULT 0,
    `favorite_count` INT DEFAULT 0,
    `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_artwork_no` (`artwork_no`),
    KEY `idx_seller_id` (`seller_id`),
    KEY `idx_status` (`status`),
    KEY `idx_category_id` (`category_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='藏品表';

-- ============================================
-- 3. 竞拍域（文档2 第2.3节竞拍状态机）
-- ============================================

CREATE TABLE `t_auction` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
    `artwork_id` BIGINT UNSIGNED NOT NULL COMMENT '藏品ID',
    `current_price` DECIMAL(12,2) NOT NULL COMMENT '当前价格',
    `status` TINYINT NOT NULL DEFAULT 0 COMMENT '0-待开始 1-进行中 2-延时中 3-已结束',
    `start_time` DATETIME NOT NULL,
    `end_time` DATETIME NOT NULL,
    `extend_count` INT DEFAULT 0 COMMENT '延时次数',
    `bid_count` INT DEFAULT 0,
    `highest_bidder_id` BIGINT UNSIGNED DEFAULT NULL,
    `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_artwork_id` (`artwork_id`),
    KEY `idx_status_end_time` (`status`, `end_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='竞拍表';

CREATE TABLE `t_auction_record` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
    `artwork_id` BIGINT UNSIGNED NOT NULL,
    `bidder_id` BIGINT UNSIGNED NOT NULL,
    `bid_price` DECIMAL(12,2) NOT NULL,
    `bid_time` DATETIME NOT NULL,
    `is_highest` TINYINT DEFAULT 0 COMMENT '是否为当前最高价',
    `biz_no` VARCHAR(32) NOT NULL COMMENT '业务单号（幂等性）',
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_biz_no` (`biz_no`),
    KEY `idx_artwork_id` (`artwork_id`),
    KEY `idx_bidder_id` (`bidder_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='出价记录表';

-- ============================================
-- 4. 资金域（文档2 第2.1节保证金状态机 + 第5.1节DDL）
-- ============================================

CREATE TABLE `t_deposit` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
    `deposit_no` VARCHAR(32) NOT NULL COMMENT '保证金单号',
    `user_id` BIGINT UNSIGNED NOT NULL,
    `artwork_id` BIGINT UNSIGNED NOT NULL,
    `amount` DECIMAL(12,2) NOT NULL,
    `status` TINYINT NOT NULL DEFAULT 0 COMMENT '0-未缴纳 1-缴纳中 2-已冻结 3-已抵扣 4-已退还 5-已没收',
    `pay_time` DATETIME DEFAULT NULL,
    `deduct_time` DATETIME DEFAULT NULL,
    `refund_time` DATETIME DEFAULT NULL,
    `forfeit_time` DATETIME DEFAULT NULL,
    `status_history` JSON DEFAULT NULL COMMENT '状态变更历史',
    `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_deposit_no` (`deposit_no`),
    KEY `idx_user_artwork` (`user_id`, `artwork_id`),
    KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='保证金表';

-- 新增：冻结明细表（文档2 第5.1节）
CREATE TABLE `t_frozen_detail` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
    `user_id` BIGINT UNSIGNED NOT NULL,
    `frozen_type` VARCHAR(20) NOT NULL COMMENT 'DEPOSIT/WITHDRAWAL/ORDER',
    `biz_id` BIGINT UNSIGNED NOT NULL,
    `biz_no` VARCHAR(32) NOT NULL,
    `amount` DECIMAL(12,2) NOT NULL,
    `status` TINYINT NOT NULL DEFAULT 1 COMMENT '1-冻结中 2-已解冻',
    `frozen_time` DATETIME NOT NULL,
    `unfreeze_time` DATETIME DEFAULT NULL,
    `expected_unfreeze_time` DATETIME DEFAULT NULL,
    `remark` VARCHAR(255),
    `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_biz_no` (`biz_no`),
    KEY `idx_user_id` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='资金冻结明细表';

-- ============================================
-- 5. 订单域（文档2 第2.2节订单状态机）
-- ============================================

CREATE TABLE `t_order` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
    `order_no` VARCHAR(32) NOT NULL COMMENT '订单号',
    `buyer_id` BIGINT UNSIGNED NOT NULL,
    `seller_id` BIGINT UNSIGNED NOT NULL,
    `artwork_id` BIGINT UNSIGNED NOT NULL,
    `order_type` TINYINT NOT NULL COMMENT '1-一口价 2-竞拍',
    `status` TINYINT NOT NULL DEFAULT 0 COMMENT '0-待付款 1-已付款 2-待鉴定 3-鉴定中 4-待发货 5-已发货 6-已收货 7-已完成 8-已结算 9-已取消 10-退款中 11-已退款',
    `total_amount` DECIMAL(12,2) NOT NULL,
    `deposit_amount` DECIMAL(12,2) DEFAULT 0 COMMENT '保证金抵扣',
    `final_amount` DECIMAL(12,2) NOT NULL COMMENT '实付金额',
    `deposit_id` BIGINT UNSIGNED DEFAULT NULL,
    `pay_time` DATETIME DEFAULT NULL,
    `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_order_no` (`order_no`),
    KEY `idx_buyer_id` (`buyer_id`),
    KEY `idx_seller_id` (`seller_id`),
    KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='订单表';
```

**产出物3：《数据库设计文档》**
- ER图（使用Navicat Data Modeler或draw.io绘制）
- 完整SQL文件（`art_platform_schema.sql`）
- 数据字典（每个字段的含义、取值范围）

---

### Day 6-7: 设计确认与接口规划

**Step 5: 使用文档3规划API**

```yaml
# 基于文档3《API与接口契约》制作接口清单

# 用户模块
POST /api/v1/user/register     # 注册
POST /api/v1/user/login        # 登录
GET  /api/v1/user/info         # 获取用户信息

# 藏品模块  
POST /api/v1/artwork/publish   # 发布藏品（卖家）
GET  /api/v1/artwork/list      # 藏品列表（搜索）
GET  /api/v1/artwork/{id}      # 藏品详情

# 竞拍模块
POST /api/v1/auction/{id}/deposit  # 缴纳保证金
POST /api/v1/auction/{id}/bid      # 出价（HTTP降级）
GET  /api/v1/auction/{id}/bids     # 出价记录
WS   /ws/auction/{id}              # WebSocket实时竞拍

# 订单模块
POST /api/v1/order/create      # 创建订单
POST /api/v1/order/{id}/pay    # 支付
GET  /api/v1/order/{id}        # 订单详情

# 支付回调
POST /api/v1/payment/callback/alipay
POST /api/v1/payment/callback/wechat
```

**产出物4：《接口清单》（Apifox格式）**

在Apifox中创建项目，导入上述接口，设置Mock规则。

---

## 💻 Week 3-6: 开发阶段（使用文档4编码）

### 前端开发（基于vben-admin改造）

**Step 6: 改造vben-admin（依据文档4第3章）**

```typescript
// 1. 修改主题色（文档4第3.1节设计系统）
// 文件：src/design/ant/index.less
@primary-color: #1a365d;      // 深靛蓝（主色）
@success-color: #38a169;      // 翠绿（真品）
@warning-color: #d69e2e;      // 琥珀金（存疑）
@error-color: #e53e3e;        // 朱红（赝品）

// 2. 新增艺术品专用CSS变量
// 文件：src/styles/artwork-theme.css
:root {
  --art-authentic: #38a169;
  --art-doubtful: #d69e2e;
  --art-fake: #e53e3e;
  --art-pending: #3182ce;
}

// 3. 创建Pinia Store（文档4第3.2节模板）
// src/stores/modules/auction.ts
import { defineStore } from 'pinia';
import { ref, computed } from 'vue';

export const useAuctionStore = defineStore('auction', () => {
  // State
  const currentAuction = ref<AuctionVO | null>(null);
  const currentPrice = ref<number>(0);
  const countdown = ref<number>(0);
  const bidRecords = ref<BidRecord[]>([]);
  
  // Getters（文档3第5.3.2节消息格式）
  const isHighestBidder = computed(() => {
    // 判断是否为最高出价者
  });
  
  const minBidPrice = computed(() => {
    return currentPrice.value + (currentAuction.value?.increment || 0);
  });
  
  // Actions
  function handleWsMessage(message: any) {
    // 处理WebSocket消息（文档3第5.3.2节）
    switch (message.type) {
      case 'PRICE_UPDATE':
        updatePrice(message.data);
        break;
      case 'EXTENDED':
        handleExtended(message.data);
        break;
      case 'OUTBID':
        showOutbidNotification();
        break;
    }
  }
  
  return {
    currentAuction, currentPrice, countdown, bidRecords,
    isHighestBidder, minBidPrice,
    handleWsMessage
  };
});

// 4. WebSocket连接管理（文档4第3.3节）
// src/composables/useWebSocket.ts
export function useWebSocket(url: string) {
  const ws = ref<WebSocket | null>(null);
  const connected = ref(false);
  const reconnectCount = ref(0);
  const MAX_RECONNECT = 5;
  const DELAYS = [1000, 2000, 4000, 8000, 16000]; // 指数退避
  
  // ... 实现连接、重连、心跳逻辑
  
  return { connected, send, onMessage };
}
```

**Step 7: 创建核心页面**

```vue
<!-- 竞拍页面：src/views/auction/AuctionRoom.vue -->
<template>
  <div class="auction-room">
    <!-- 藏品信息 -->
    <ArtworkCard :artwork="artwork" />
    
    <!-- 实时竞价区 -->
    <div class="bid-section">
      <div class="current-price">
        当前价：{{ formatMoney(currentPrice) }}
      </div>
      <div class="countdown">{{ formatTime(countdown) }}</div>
      
      <!-- 出价按钮（权限控制：文档3第6章） -->
      <button 
        v-permission="'AUCTION:BID'"
        :disabled="!canBid || isBidding"
        @click="openBidModal"
      >
        {{ isBidding ? '出价中...' : '立即出价' }}
      </button>
    </div>
    
    <!-- 出价记录 -->
    <BidRecordList :records="bidRecords" />
  </div>
</template>

<script setup>
import { useAuctionStore } from '@/stores/modules/auction';
import { useWebSocket } from '@/composables/useWebSocket';
import { formatMoney, formatTime } from '@/utils/format'; // 文档4第4章

const route = useRoute();
const store = useAuctionStore();

// 建立WebSocket连接（文档3第5.1节）
const { connected, send, onMessage } = useWebSocket(
  `wss://api.artplatform.com/ws/auction/${route.params.id}`
);

// 监听消息
onMessage((msg) => {
  store.handleWsMessage(msg);
});

// 出价方法
const placeBid = (price) => {
  if (!connected.value) {
    // WebSocket断开时使用HTTP降级（文档3第4.3.1节）
    httpPlaceBid(price);
    return;
  }
  
  store.isBidding = true;
  send({ 
    type: 'BID', 
    price, 
    bizNo: generateBizNo(), // 幂等性键（文档3第7章）
    timestamp: Date.now() 
  });
};
</script>
```

### 后端开发（基于mall改造）

**Step 8: 改造mall项目（依据文档4第2章）**

```java
// 1. 修改包结构：com.macro.mall → com.art.platform

// 2. 创建竞拍服务（文档4第2.5.3节模板）
// src/main/java/com/art/platform/service/impl/AuctionServiceImpl.java

@Service
@Slf4j
public class AuctionServiceImpl implements AuctionService {
    
    @Autowired
    private StringRedisTemplate redisTemplate;
    
    @Autowired
    private RedissonClient redissonClient; // Redis分布式锁
    
    @Autowired
    private AuctionMapper auctionMapper;
    
    @Autowired
    private BidRecordMapper bidRecordMapper;
    
    @Autowired
    private RabbitTemplate rabbitTemplate;
    
    /**
     * 竞拍出价（文档1第4.1.2节核心代码 + 文档4第2.5.3节）
     */
    @Override
    public BidResultVO placeBid(Long artworkId, Long userId, BigDecimal bidPrice) {
        String lockKey = "lock:auction:bid:" + artworkId;
        RLock lock = redissonClient.getLock(lockKey);
        
        try {
            // 1. 获取分布式锁（文档1 ADR-003）
            boolean acquired = lock.tryLock(3, 5, TimeUnit.SECONDS);
            if (!acquired) {
                throw new BizException("B2002", "系统繁忙，请稍后重试");
            }
            
            // 2. 检查保证金（文档2第2.1节状态机）
            if (!hasValidDeposit(artworkId, userId)) {
                throw new BizException("B2001", "请先缴纳保证金");
            }
            
            // 3. Lua脚本原子更新（文档1第4.1.2节）
            String luaScript = 
                "local current = tonumber(redis.call('hget', KEYS[1], 'currentPrice'));" +
                "local increment = tonumber(redis.call('hget', KEYS[1], 'increment'));" +
                "local minPrice = current + increment;" +
                "if tonumber(ARGV[1]) < minPrice then return {-1, minPrice}; end;" +
                "redis.call('hset', KEYS[1], 'currentPrice', ARGV[1]);" +
                "redis.call('hset', KEYS[1], 'highestBidder', ARGV[2]);" +
                "local count = redis.call('hincrby', KEYS[1], 'bidCount', 1);" +
                "return {1, count};";
            
            // ... 执行Lua脚本
            
            // 4. 异步保存出价记录（文档2第3.2节并发控制）
            BidMessage message = new BidMessage();
            message.setArtworkId(artworkId);
            message.setBidderId(userId);
            message.setBidPrice(bidPrice);
            rabbitTemplate.convertAndSend("auction.exchange", "bid.record", message);
            
            // 5. WebSocket广播（文档3第5.3.2节）
            broadcastPriceUpdate(artworkId, bidPrice, userId);
            
            return BidResultVO.builder()
                .success(true)
                .currentPrice(bidPrice)
                .build();
                
        } catch (InterruptedException e) {
            Thread.currentThread().interrupt();
            throw new BizException("C5001", "出价被中断");
        } finally {
            if (lock.isHeldByCurrentThread()) {
                lock.unlock();
            }
        }
    }
}
```

**Step 9: 状态机实现（文档2第2章）**

```java
// 保证金状态机（文档2第2.1节）
@Service
public class DepositStatusService {
    
    @Autowired
    private DepositMapper depositMapper;
    
    @Autowired
    private WalletService walletService;
    
    /**
     * 状态流转（带会计分录）
     */
    @Transactional
    public void transition(Long depositId, DepositStatus from, DepositStatus to, 
                          Long operatorId, String reason) {
        // 1. 乐观锁更新状态
        int affected = depositMapper.updateStatus(depositId, from.getCode(), to.getCode());
        if (affected == 0) {
            throw new BizException("状态流转失败：当前状态非" + from);
        }
        
        // 2. 资金操作（文档2第2.1节会计分录）
        switch (to) {
            case FROZEN:
                // 借：可用余额  贷：冻结金额
                walletService.freeze(deposit.getUserId(), deposit.getAmount());
                break;
            case DEDUCTED:
                // 借：冻结金额  贷：订单支付冻结
                walletService.transferFrozen(deposit.getUserId(), 
                    PLATFORM_ACCOUNT, deposit.getAmount());
                break;
            case REFUNDED:
                // 借：冻结金额  贷：可用余额
                walletService.unfreeze(deposit.getUserId(), deposit.getAmount());
                break;
            case FORFEITED:
                // 借：冻结金额  贷：平台收入 + 卖家补偿
                handleForfeit(deposit);
                break;
        }
        
        // 3. 记录状态历史
        recordStatusHistory(depositId, from, to, operatorId, reason);
    }
}
```

---

## 🔍 Week 7-8: 联调测试（使用文档3+文档5）

### Step 10: 接口联调（依据文档3）

```bash
# 使用Apifox进行接口测试
# 1. 导入Swagger文档（后端自动生成）
# 2. 设置环境变量（dev/test/prod）
# 3. 编写测试用例

# 测试用例示例（文档3第9章检查清单）
Test Case: 竞拍出价并发测试
├── Step 1: 用户A缴纳保证金
├── Step 2: 用户B缴纳保证金  
├── Step 3: 用户A出价10000
├── Step 4: 用户B同时出价10000（预期：一人成功，一人失败B2005）
└── Assert: 价格单调递增，无重复出价记录
```

### Step 11: 并发压测（文档5红线3）

```bash
# 使用JMeter执行50并发压测（文档5第3.1节脚本）
./run-concurrent-test.sh

# 预期结果：
# - 成功率 ≥ 99.9%
# - P99延迟 < 500ms
# - 无超卖、无重复出价
```

---

## ✅ Week 9-10: 验收上线（使用文档5）

### Step 12: 执行三条红线检查

```bash
# 一键验收命令（文档5第3.1节）
./check-all.sh

# 输出示例：
==========================================
艺术品交易系统 - 上线前验收
==========================================

【红线1】资金操作成功率 = 100%...
✅ 红线1通过（无失败资金操作）

【红线2】日终对账5项SQL...
✅ 红线2通过（5项检查全部通过）

【红线3】50并发压测...
成功率: 99.95% (目标: ≥99.9%)
P99延迟: 320ms (目标: <500ms)
✅ 红线3通过

==========================================
✅ 全部红线通过，允许上线
==========================================
```

**产出物5：《上线验收报告》**（文档5格式）

---

## 📚 文档使用速查表

| 开发阶段   | 使用文档 | 具体章节                       | 产出物                  |
| :--------- | :------- | :----------------------------- | :---------------------- |
| 环境搭建   | 文档1    | 2.2部署架构图                  | 环境检查报告            |
| 数据库设计 | 文档2    | 第1章ER图 + 第5章DDL           | 数据库设计文档、SQL文件 |
| 接口设计   | 文档3    | 第4章接口清单 + 第5章WebSocket | Apifox接口文档          |
| 编码实现   | 文档4    | 第2章Java模板 + 第3章Vue模板   | 功能模块代码            |
| 状态机实现 | 文档2    | 第2章五大状态机                | 状态流转代码            |
| 并发控制   | 文档1    | 4.1.2核心代码 + ADR-003        | 分布式锁+Lua脚本        |
| 联调测试   | 文档3    | 第9章检查清单                  | 测试用例、Bug清单       |
| 验收上线   | 文档5    | 第2章三条红线                  | 验收报告、部署文档      |

---

## 📁 项目文件结构

```
artwork-platform/
├── 📄 文档层（5个核心文档）
│   ├── 文档1-系统架构与核心决策.md
│   ├── 文档2-数据模型与状态机.md
│   ├── 文档3-API与接口契约.md
│   ├── 文档4-编码规范与实现模板.md
│   └── 文档5-验收红线与自动化.md
│
├── 🎨 前端（vben-admin改造）
│   ├── src/
│   │   ├── views/artwork/      # 藏品相关页面
│   │   ├── views/auction/      # 竞拍页面（含WebSocket）
│   │   ├── views/order/        # 订单中心
│   │   ├── stores/modules/     # Pinia状态管理
│   │   ├── composables/        # WebSocket组合式函数
│   │   └── utils/format.ts     # 金额/状态格式化
│   └── package.json
│
├── ⚙️ 后端（mall改造）
│   ├── mall-admin/
│   │   ├── src/main/java/com/art/platform/
│   │   │   ├── controller/     # 控制器（权限注解）
│   │   │   ├── service/impl/   # 服务实现（状态机）
│   │   │   ├── mapper/         # MyBatis-Plus（XML转义！）
│   │   │   └── websocket/      # WebSocket处理器
│   │   └── pom.xml
│   └── sql/
│       └── art_platform_schema.sql  # 数据库脚本
│
├── 🗄️ 数据库
│   ├── ER图.pdf
│   └── 数据字典.xlsx
│
├── 🔧 运维脚本（文档5）
│   ├── check-all.sh            # 一键验收
│   ├── check-daily.sql         # 日终对账SQL
│   └── run-concurrent-test.sh  # 压测脚本
│
└── 📊 进度汇报
    ├── Week1-环境搭建完成.md
    ├── Week2-数据库设计评审.md
    ├── Week4-前端框架改造完成.md
    ├── Week6-后端核心模块完成.md
    ├── Week8-联调测试报告.md
    └── Week10-上线验收报告.md
```

---

## 🎯 汇报进度表模板

| 周次       | 阶段 | 完成任务                    | 使用文档                    | 产出物            | 风险与对策                    |
| :--------- | :--- | :-------------------------- | :-------------------------- | :---------------- | :---------------------------- |
| Week 1     | 准备 | Node/JDK/MySQL等9个软件安装 | 文档1第2章                  | 环境检查报告      | 版本不匹配→严格按文档1版本    |
| Week 2     | 准备 | 脚手架部署+数据库设计       | 文档2第1-2章                | ER图+SQL文件      | 表关系复杂→对照文档2状态机    |
| Week 3     | 开发 | 前端环境+藏品列表页         | 文档4第3章                  | 可运行的列表页    | vben组件不熟悉→复用BasicTable |
| Week 4     | 开发 | 后端藏品CRUD+OSS上传        | 文档4第2.5.2节              | Swagger接口文档   | 文件上传失败→检查OSS配置      |
| Week 5     | 开发 | 竞拍页面+出价接口           | 文档1第4.1节+文档3第5章     | WebSocket连接成功 | 并发问题→Redisson锁           |
| Week 6     | 开发 | 订单流程+支付对接           | 文档2第2.2节+文档4第2.5.4节 | 完整交易流程      | 回调丢失→幂等性设计           |
| Week 7     | 测试 | 接口联调+单元测试           | 文档3第9章                  | 测试用例100%通过  | 状态不一致→执行日终SQL        |
| Week 8     | 测试 | 50并发压测                  | 文档5红线3                  | 压测报告          | 成功率不足→优化Redis Lua      |
| Week 9     | 验收 | 三条红线检查                | 文档5第2章                  | 验收报告          | 资金异常→人工审计             |
| Week 10    | 上线 | 部署+监控                   | 文档1第2.2节                | 线上运行          | 故障→四级降级策略             |
| Week 11-12 | 论文 | 文档整理                    | 全部                        | 毕业论文          | -                             |

---

**核心口诀**：
- **设计时**：文档2在手（数据不会错）
- **编码时**：文档4在手（代码直接抄）
- **联调时**：文档3在手（接口能对上）
- **上线前**：文档5在手（三条红线过）
- **全程**：文档1指导（架构不跑偏）

需要我针对任何具体步骤展开更详细的操作说明吗？例如：
1. vben-admin的具体改造步骤
2. mall项目的RBAC权限改造
3. WebSocket+Redis的具体实现
4. 日终对账SQL的编写